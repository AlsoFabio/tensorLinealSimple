{
  "name": "tfjs-examples-micro",
  "version": "0.1.0",
  "description": "This minimal example loads tfjs from a CDN, builds and trains a minimal model,\r and uses it to predict.  Edit `index.js` and load `index.html` in your\r browser to test small snippets.",
  "main": "index.js",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": ">=8.9.0"
  },
  "scripts": {
    "watch": "npm run build && node_modules/http-server/bin/http-server dist -p 1234 ",
    "build": "mkdir -p dist/ && cp index.html dist/ && cp index.js dist/",
    "dev": "node index.js"
  },
  "devDependencies": {
    "clang-format": "^1.8.0",
    "http-server": "^14.1.1"
  },
  "resolutions": {
    "follow-redirects": "1.14.8"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^4.5.0",
    "express": "^4.18.2"
  },
  "author": ""
}
